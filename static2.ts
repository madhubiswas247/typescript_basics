class StaticClass2 {
    private static x = 0;
  }
  console.log(StaticClass2.x);

  //Property 'x' is private and only accessible within class 'MyClass'.
  //Static members can also use the same public, protected, and private visibility modifiers: